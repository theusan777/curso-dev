/*
    == ---- igual ---- a==b ----verdadeiro se a for igual a b
    === ---- estritamente igual ---- a===b ---- verdadeiro se a for igual a b e do mesmo tipo
    !== ---- estritamente diferente ---- a!==b ---- verdadeiro se a for diferente de b e do mesmo tipo
    != ---- diferente ---- a!=b ---- verdadeiro se a for diferente de b
    > ---- maior que ---- a>b ---- verdadeiro se a for maior que b  
    < ---- menor que ---- a<b ---- verdadeiro se a for menor que b
    >= ---- maior ou igual a ---- a>=b ---- verdadeiro se a for maior ou igual a b
    <= ---- menor ou igual a ---- a<=b ---- verdadeiro se a for menor ou igual a b
    && ---- e ---- a&&b ---- verdadeiro se a e b forem verdadeiros   
    || ---- ou ---- a||b ---- verdadeiro se a ou b forem verdadeiros
    ! ---- não ---- !a ---- verdadeiro se a for falso   
    //    !a ---- verdadeiro se a for falso   
    
*/

const a = 10;
const b = 10;   

console.log(a==b)