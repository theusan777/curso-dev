let frutas = ['Banana', 'Pêra', 'Uva'];

let videoGame = {
    nome: 'PS5',
    preco: 4500,
    usado: false,
} 


let videoGames = [videoGame, videoGame2, videoGame3]; 
console.log(videoGames);

console.log(videoGames[1].nome); 
     

